export function DrawOneWordWorksheet() {
  return (
    <p className="text-sm text-muted-foreground">
      When Dictation is included, students pick one word from their dictated word
      list, write it on the line, and draw a picture that shows what the word
      means. This section appears on the printable worksheet below Dictation when
      both are selected.
    </p>
  )
}
